# Simple Task Manager

## Overview
The Simple Task Manager is a shell script that allows you to manage a to-do list from the command line. You can add tasks, view them, and remove them.

## Usage
1. Run the script using the command:
   ```bash
   bash task_manager.sh
